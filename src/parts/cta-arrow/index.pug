mixin cta-arrow(ctaLink, ctaText)
    if ctaLink
        a.cta-arrow(href=ctaLink)&attributes(attributes)
            i.fas.fa-arrow-right
            span!= ctaText
    else
        span.cta-arrow(href=ctaLink)&attributes(attributes)
            i.fas.fa-arrow-right
            span!= ctaText